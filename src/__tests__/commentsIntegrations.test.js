// This file would be used if our app grew to the point of having many integration tests. This file would be where all comment integration tests would be housed.

it('commentsIntegration placeholder', () => {
	expect(1).toEqual(1);
});
